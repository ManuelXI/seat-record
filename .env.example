# Optional. Without a key, scripted demo entries use cached responses and anything else gets a labelled plain draft.
ANTHROPIC_API_KEY=
# Optional. Overrides the committed demo signing key (PKCS#8 PEM, Ed25519).
SIGNING_PRIVATE_KEY_PEM=
